env: python
# ffmpeg is a runtime binary the music cog shells out to — pip cannot install it.
buildCommand: apt-get update && apt-get install -y ffmpeg && pip install -r requirements.txt
startCommand: python main.py
